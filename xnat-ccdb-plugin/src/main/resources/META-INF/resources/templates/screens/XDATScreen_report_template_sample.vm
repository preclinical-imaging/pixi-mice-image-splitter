$page.setTitle("Sample")
$page.setLinkColor($ui.alink)
$page.setVlinkColor($ui.vlink)

#macro(showBoolean $property $item)
<!-- BEGIN Generated by the showBoolean macro -->
    #set ($value = $!item.getBooleanProperty($property))
    #if( "$!value" == "" )
    (No value given)
    #elseif ($value)
    Yes
    #else
    No
    #end
<!-- END Generated by the showBoolean macro -->
#end

#if ($data.getParameters().getString("popup"))
	#set ($popup = $data.getParameters().getString("popup") )
	#set ($popup = "false")
#end
<table width="100%">
	<tr>
		<td>
			#parse($turbineUtils.getTemplateName("_report",$om.getXSIType(),$project))
		</td>
		<td valign="top" align="right">
			#elementActionsBox($element $search_field $search_value $data.getSession().getAttribute("user") $item)
		</td>
	</tr>
	<tr>
		<td>
			<table>
                <tr>
                    <td colspan="3" style="padding-top: 20px; padding-bottom: 10px; text-align: left; vertical-align: middle;">
                        <span style="font-family: $ui.sansSerifFonts; font-size: small; "><b>Sample</b></span>
                    </td>
                </tr>
                <tr>
                    <td>&nbsp;</td>
                    #formLabel("Pass?")
                    <td valign="top">
                        #showBoolean("template:sample/pass" $item)
                    </td>
                </tr>
                <tr>
					<td valign="top">
						#parse("/screens/ReportProjectSpecificFields.vm")
					</td>
				</tr>
			</table>
		</td>
	</tr>
</table>
